# Linux 部署黑马点评 -踩坑记录

**日期**: 2026-06-17

## **环境**
- 系统: Ubuntu 26.04 LTS
- 部署方式: 手动部署

- ## 问题1:基础环境、软件安装层
- **MySQL 安装**: 因先前安装/启动了MySQL进程,导致apt安装检测冲突
- **解决方案**: 先清理先前安装/运行的

- **Redis 配置文件丢失**: sudo nano /etc/redis/redis.conf 打开了一个空白新文件，说明之前根本没生成配置文件（或者路径不对）
- **解决方案**: 查询先前是否有新的redis在文件另外的地方，没有则生成新的配置文件/重新导入

- ## 问题2: 端口 || 防火墙网络层("外部访问不了")
- **MySQL无法远程连接**: bind-address 绑死在 127.0.0.1 没有换监视端口
- **端口被占(点名8081)**: 就的Java进程长时占用端口,导致新端口执行不起来,报错:Address already in user
-  **Nginx监听错端口**: 配置默认listen 8080 ,项目默认访问80端口

-  ## 🐖问题3: 静态资源层 || 权限
-  **Nginx 403 权限拒绝**: /home/orunji/humdp 目录权限不足Nginx用户进不去
-  **解决方法**: 放行该用户
  
-  **Nginx 404 找不到文件**:上传的前端文件路径多了一层 nginx-1.18.0/html/，导致 root 指向错误
-   **解决方法**:

  
-  **静态资源无法加载**:子目录（CSS/JS）权限没递归设置，导致网页样式全丢
-   **解决方法**:

- ## 问题4:后端服务启动层（“后端 Java 跑不起来”）
-  **后端配置文件里的 IP 格式错误**: xxx:xxx:xxx:xxx（用了冒号 : 而不是点 .），导致数据库和 Redis 根本无法建立物理连接。

- ## 问题5:路径代理与转发层（“前后端数据对不上”——今天最核心、最长的拉锯战!!!!!!!!!!!!）
- **前端请求无前缀**: 前端发 GET /shop-type/list，但后端只认 GET /api/shop-type/list
- **解决方法**:

- **Nginx 代理加前缀**: 尝试用 rewrite 强行补上 /api
-   **解决方法**:
  
- **Nginx 正则误伤静态文件**: 写了一个 location ~ ^/(shop|blog|...) 想要代理接口，结果把 shop-list.html 这个静态页面也拦截了，导致点击分类跳转时返回 404（而不是页面）
-   **解决方法**:
